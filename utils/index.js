const QuoteGenerate = require('./quote-generate')
const loadImageFromUrl = require('./image-load-url')
const loadImageFromPath = require('./image-load-path')
const userName = require('./user-name')

module.exports = {
  QuoteGenerate,
  loadImageFromUrl,
  loadImageFromPath,
  userName
}
