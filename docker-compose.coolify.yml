# Coolify deployment compose for quote-api (hetzner-auction).
# Internal API consumed by quote-bot via host port 4888 (no public domain).
services:
  quote-api:
    build:
      context: .
      dockerfile: Dockerfile.coolify
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "4888:4888"
