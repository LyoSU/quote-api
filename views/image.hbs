<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <link rel="stylesheet" href="https://arebaka.github.io/css/reset.min.css" />
  <style>
    body {
      background: transparent;
    }
    #quote {
      position: absolute;
      padding: 35px 55px;
      max-width: 512px;
      width: {{width}}px;
      height: {{height}}px;
      //transform: scale({{scale}});
      background:
        url("{{background.image.url}}") top left / cover repeat,
        radial-gradient(ellipse farthest-corner at center, {{background.color2}}, {{background.color1}});
    }
    .messages-list {
      list-style: none;
    }
    .message-item {
      display: block;
      margin-bottom: 5px;
    }
    .message-item:last-child {
      margin-bottom: 0;
    }
    .message-from-avatar {
      display: block;
      float: left;
      margin-left: 5px;
      width: 50px;
      height: 50px;
      font: 25px/2 'Nono Sans', sans-serif;
      text-align: center;
      color: #FFF;
      border-radius: 50%;
      box-shadow: 8px 8px 13px rgba(0, 0, 0, .5);
    }
    .message {
      margin-left: 65px;
      padding: 14px;
      background: linear-gradient(-45deg, {{background.color1}}, {{background.color2}});
      border-radius: 25px;
      box-shadow: 8px 8px 13px rgba(0, 0, 0, .5);
    }
    .message-from-name {
      overflow: hidden;
      width: 100%;
      font: bold 22px/1.2 'Noto Sans', sans-serif;
      white-space: nowrap;
      text-overflow: ellipsis;
    }
    .message-text {
      overflow: hidden;
      width: 100%;
      font: 24px/1.2 'Noto Sans', sans-serif;
      color: #FFF;
      mix-blend-mode: difference;
      text-overflow: ellipsis;
    }
    .watermark {
      position: absolute;
      bottom: 5px;
      right: 5px;
      font: 12px 'Noto Sans';
      text-align: right;
      color: rgba(0, 0, 0, .3);
    }
  </style>
</head>
<body class="{{theme}}">
  <main id="quote">
    <ul class="messages-list">
    {{#each messages}}
      <li class="message-item">
        <img
          src="{{this.from.avatar.url}}" alt="" class="message-from-avatar"
          style="background-color: {{this.from.color}};" />
        <article class="message">
          <header>
            <p class="message-from-name" style="color: {{this.from.color}}" >
              {{this.from.name}}
              <span class="message-from-emoji_status"></span>
            </p>
          </header>
          <main>
            <p class="message-text">{{this.text}}</p>
          </main>
        </article>
      </li>
    {{/each}}
    </ul>
    <p class="watermark"> @QuotLyBot </p>
</body>
<html>
